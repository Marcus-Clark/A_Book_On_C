#include <stdio.h>

int main(void)
{
   int   a, b = 2, c = 3;     /* a, b, and c will be used
                                to illustrate arithmetic/*

   a = (4 * b + 5 * c) / 6
   printf("a = %d   b = %d   c = %d\n", a, b, c);
   return 0;
)
